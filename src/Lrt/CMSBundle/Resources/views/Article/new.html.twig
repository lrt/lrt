{% extends ':Admin:base.html.twig' %}

{% block body %}
<div class="span12">
    <h4 class="header">Ajouter un article</h4>
    {% form_theme form ':Themes:form.html.twig' %}
    <form action="{{ path('article_create') }}" method="post" {{ form_enctype(form) }} class="form-horizontal">
        {{ form_widget(form) }}
        <div class="form-actions">
            <button type="submit" class="btn btn-primary"><i class="icon-check icon-white"></i> Valider</button>
            <a href="{{ path('article') }}" class="btn">
                <i class="icon-list"></i> Retour à la liste
            </a>
        </div>
    </form>
</div>
{% endblock %}
   
{% block javascripts %} 
   {{ parent() }}
    <script type="text/javascript" src="{{ asset('bundles/cms/js/plugins/ckeditor/ckeditor.js') }}"></script>
    <script type="text/javascript">
        $('#lrt_cmsbundle_articletype_content').removeAttr('required');
        //<![CDATA[
        var editor = CKEDITOR.replace('lrt_cmsbundle_articletype[content]', {
            uiColor: '#FAFAFA'
        });
        //]]>   
    </script>    
{% endblock %}    

   